# plan.md 템플릿

> 인간이 읽고 피드백하는 문서. 에이전트 실행 관련 정보(참조 스킬, 맥락, 위임 프롬프트 등)는 포함하지 않는다.

````markdown
# 계획 제목

## 요약

**뭘 하는 건지**: (한 문장으로 작업 목표)

**어떻게 하는 건지**:

1. (Step 1 한 줄 설명)
2. (Step 2 한 줄 설명)
3. ...

**뭐가 바뀌는 건지**: 신규 N개, 수정 N개, 삭제 N개

## 개요

- 작업 목표 요약

## 배경/컨텍스트

- 왜 이 작업이 필요한지

## 영향 범위

> 파일 목록은 임의로 정하지 않는다. **참조 스킬이 정의한 파일 구조·위치·네이밍 규칙**에 따라 도출한다.
> 스킬에 규칙이 없는 경우에만 기존 코드베이스의 컨벤션을 참고한다.

### 파일 변경 목록

`+` 생성 · `*` 수정 · `-` 삭제

```
apps/dashboard/src/
├── apis/
│   └── + use-new-query.ts            # 새 API 훅
├── components/
│   └── + new-component.tsx          # 새 컴포넌트
├── app/
│   └── * page.tsx                   # 라우트 추가
└── messages/
    └── * ko.json                    # 번역키 추가
```

## 결정 사항

- (질문/답변 과정에서 확정된 결정을 요약하여 기록)

## 구현 단계

### Step 1: [단계 제목]

**목표**: 이 단계에서 달성할 것

**작업 내용**:

- [ ] 세부 작업 1
- [ ] 세부 작업 2

---

### Step 2: [단계 제목]

**목표**: 이 단계에서 달성할 것

**작업 내용**:

- [ ] 세부 작업 1
- [ ] 세부 작업 2

---

(필요한 만큼 Step 추가)

## 고려사항

- 주의할 점, 엣지 케이스

## 추가 요청 사항

- (사용자가 구현 시 반영해야 할 추가 요청을 자유롭게 기록)
- 예: "기존 OO 컴포넌트의 패턴을 따라서 구현해줘"
- 예: "XX 라이브러리는 사용하지 말아줘"
- 예: "에러 메시지는 한국어로만 처리해줘"
- 예: "이 페이지는 모바일 대응 안 해도 돼"
````
